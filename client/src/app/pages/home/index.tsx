import React from 'react';
import { Box, Text } from '@chakra-ui/core';

interface IProps {}

export const HomePage: React.FC<IProps> = () => {
    return (
        <Box>
            <Text>Home Page</Text>
        </Box>
    );
};
